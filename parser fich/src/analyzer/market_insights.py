"""Market insights: patterns, UX flows, differentiation, underserved segments."""
from __future__ import annotations

import logging
from typing import TYPE_CHECKING

if TYPE_CHECKING:
    pass

logger = logging.getLogger(__name__)


def generate_insights(features: list, mechanics: list) -> list[str]:
    """
    Build insights from features and mechanics: common patterns, standard UX,
    differentiation opportunities, underserved segments.
    Stub: returns empty list.
    """
    logger.info("Market insights (stub): %s features, %s mechanics", len(features), len(mechanics))
    return []
